# Website Changelog

## 0.1.0 - 2026-07-05

- Started Phase 1 website foundation.
- Added Next.js, React, TypeScript, Tailwind CSS, and app-router project structure.
- Added premium mobile repair homepage with service, booking, quote, insurance, door-to-door, and admin entry points.
- Added customer dashboard and admin dashboard foundation pages.
- Added login, register, forgot password, track repair, booking, quote, insurance quote, services, pricing, FAQ, contact, policy, terms, blog, and 404 pages.
- Added planning documentation for architecture, database schema, API design, authentication, deployment, and security.
- Updated Nodemailer dependency to the fixed 9.x line after the first dependency audit.
- Added the first functional local MVP layer: booking requests, quote requests, repair tracking, admin status updates, admin notes, quoted prices, and editable repair pricing persist in browser local storage.
- Connected the public pricing page to the editable admin price list.
- Connected the customer dashboard to the repair/quote tracking component.
- Replaced password/register/reset UX with passwordless email OTP login. New and returning customer emails use the same OTP verification flow; the local MVP shows the OTP on screen until SMTP email sending is connected.
- Fixed local MVP state migration so browsers with older saved data automatically receive the new `users` field required by OTP login.
- Added the Next.js smooth-scroll marker to remove the dev overlay warning for `scroll-behavior: smooth`.
- Updated auth navigation: header shows `Login` when logged out and `Logout` when logged in; OTP login screen is centered.
- Added mobile number capture to the OTP login flow for first-time customers and added registered users to the admin dashboard.
- Split admin into a separate back-office shell: public header/footer are hidden on `/admin/*`, and the admin dashboard now uses its own sidebar/topbar layout.
- Split authentication into proper Login and Register flows. Login now checks that the email already exists; Register collects email, mobile number, and OTP, then logs the user in.
- Replaced plain auth feedback text with styled warning/success alerts and clickable Register/Login actions.
- Restricted registration phone validation to Australian mobile numbers only.
- Changed successful OTP login/register redirect to the home page.
- Replaced the placeholder `C` brand mark with the Casey logo structure: dark circular badge, white phone outline, lime bolt, CASEY wordmark, and spaced tagline.
- Rebuilt the Casey logo mark as SVG so the lightning bolt renders lime instead of the system emoji color.
- Refined the header: resized the Casey logo for navbar alignment and upgraded navigation with glass styling, hover animation, active states, and richer button motion.
- Enhanced navbar animation with header glow, active-link sheen, pulsing lime underline, logo hover pulse, and primary-button shine.
- Rebuilt the Services page with animated hero/orbit visual, animated service cards, hover glow effects, and added Refurbished laptops and mobiles plus IT support.
- Rebuilt the Pricing page into a professional pricing experience with a distinct hero, estimate meter, featured cards, searchable/filterable live pricing, and trust notes.
- Renamed Pricing navigation/page to Repair Cost Pricing and redesigned it into one cohesive all-in-one pricing panel.
- Expanded the local repair cost catalogue across iPhone, Samsung Galaxy S/Ultra/Fold/Flip, Pixel devices, and common mobile repair issues. Improved public pricing search and split admin into separate Overview, Users, Bookings, Quotes, and Pricing pages with animated pricing management.
- Shortened the navbar label to `Repair Costs` and prevented nav labels from wrapping inside active pills.
- Expanded pricing coverage to older Samsung S8 onward and more Android brands including Oppo, Vivo, Realme, Xiaomi, Redmi, POCO, OnePlus, Motorola, Huawei, Nokia, Sony, Nothing, and Asus. Replaced ugly diagnostic wording with customer-friendly diagnostic prices and made seeded admin deletions persist.
- Optimized pricing lists with pagination, global diagnostic records, improved Oppo/Samsung search examples, and cleaner diagnostic display styling.
- Changed diagnostic/inspection pricing to `$39` and added motherboard inspection/diagnostic required pricing.
- Added a Repair Costs fallback flow for customers who cannot find their device or do not know the model, including model-finding guidance and a custom estimate request that creates an admin quote.
- Added a call-us fallback message for customers who do not know any device details, with a placeholder phone number to update later.
- Customer custom estimate form now creates an Admin Query, includes a mailto reply action, and is visually separated from pagination with extra spacing.
- Removed the extra vertical gap before the custom estimate form while keeping the improved form card alignment.
- Removed per-device motherboard inspection entries so motherboard pricing appears once as a global diagnostic item.
- Fixed admin pricing edits so updated catalogue prices persist and refresh on the public Repair Costs page.
- Rebuilt the Door-to-door page into a professional mobile repair service page with animated route/workflow visuals, service options, and a working pickup/onsite request form.
- Door-to-door requests now create admin bookings with service type, address, suburb, postcode, and access notes visible in admin booking views.
- Expanded the door-to-door workflow with optional customer photos, admin deposit/balance/ETA controls, customer notification text, payment confirmation steps, pickup/route/device-received/fixed/delivery statuses, and a richer customer tracking timeline.
- Refined door-to-door dispatch so admin edits are drafted first, then saved with `Update details`, and only published to the customer with `Send to customer`; customer tracking now shows a single clear payment/ETA next action with Stripe-style deposit and balance buttons.
- Removed customer-facing door-to-door service-plan selection, added broken-device identification clues, and changed admin pricing to total amount plus deposit with automatic remaining balance shown to admin and customer.
- Added admin delete for booking requests, changed pickup/drop-off ETA to date-time pickers, and tightened the customer tracking balance/progress layout.
- Simplified the door-to-door customer form into a calmer guided request with short sections, quick issue chips, broken-device model clues, compact optional photos, and reduced explanatory text.
- Added a clear door-to-door choice between calling and filling the quick form, softened form typography/helper text, and reduced visual weight for stressed customers.
- Further optimized door-to-door intake so the form is hidden behind a clear customer choice, only essential fields show by default, and device-model help appears on demand.
- Rebuilt Insurance Quote into a professional claim-document workflow with customer photo intake, admin approval/payment controls, customer tracking, and downloadable PDF-style insurance quote documents after payment.
- Fixed insurance payment release flow so customer payment is submitted for admin confirmation first; admin now controls PDF release. Upgraded the generated insurance quote PDF into a branded invoice-style document with invoice number, business/customer/device/assessment sections, quote amount, declaration, and signature area.
- Added itemized insurance quote repair parts and costs, with admin-entered final assessment lines, auto-calculated repair total, customer tracking breakdown, and PDF wording that presents the document as the final repair assessment from submitted photos/details.
- Corrected the insurance PDF Casey logo mark and simplified the PDF total styling so the document is cleaner and more branded.
- Rebuilt the homepage into a more professional conversion-focused landing experience with clearer repair paths, trust signals, animated repair console, service cards, workflow steps, and stronger customer CTAs.
- Added a live homepage repair cost checker so customers can select a device and issue to see competitive price estimates before opening the full Repair Costs page.
- Reworked the homepage cost checker into a typeahead model search with suggestions, unknown-device fallback, and laptop/MacBook repair cost coverage.
- Rebuilt the Book Repair page around the real business model: customers can choose to visit Casey Repairs during 9 AM to 12 PM availability or request Casey to come to them at a selected date and time.
- Rebuilt the Contact page with a stronger hero, visit/pickup/insurance contact paths, workshop availability card, and a functional enquiry form that saves to Admin Queries.
- Added new top navigation tabs and full pages for IT Services and Refurbished Devices, including customer-facing support, replacement-device, and setup guidance.
- Replaced the homepage cost checker with an animated Casey repair agent that guides customers through repair pricing, door-to-door pickup, postage help, workshop visits, and unknown-device support.
- Simplified the homepage repair agent issue flow with a natural-language problem input, broad quick presets, and only the best matching repair suggestions instead of listing every issue.
- Reworked the homepage repair agent into a simpler 30-Minutes-Fix-style starter: device search, problem input, instant estimate, and three clear paths for visit, pickup, or postage.
- Benchmarked public Apple/Samsung repair pricing from 30 Minutes Fix-style APIs, reduced matched repair prices by `$50`, and updated Casey seed pricing ranges with a local-data migration so cached catalogue prices refresh.
- Added Phonebot-sourced refurbished stock examples to the Refurbished Devices page with product photos, Casey pricing set to source price plus `$50`, warranty notes, and availability enquiry actions.
- Expanded Refurbished Devices into a shop-style flow with 228 imported Phonebot public listing products, local product images, Casey pricing with `$50` margin, search/filter/pagination, Buy Now checkout, optional add-ons, delivery/pickup details, payment-style confirmation, and admin booking creation.
- Added a new SEO-friendly repair booking flow at `/repair`, `/repair/[brand]/[model]`, and `/repair/[brand]/[model]/[repair]` with brand/model picture selection, repair choices, screen/battery quality options, live booking summary, mail-in/pickup/visit service options, optional shipping-label assistance, payment-style confirmation, and admin booking creation. Updated the homepage and main Book Repair navigation to start from this cleaner flow.
- Analyzed 30minutesfix.com's repair page for layout/trust-signal patterns and added two new sections to the device-selected repair view (`repair-flow.tsx`), keeping the existing black/white/lime brand palette: a trust strip (rating badge, "trusted local repairs" copy, warranty pill) below the device header, and a 4-item "why choose Casey" row (certified technicians, fair pricing, warranty, fast turnaround) after the repair card grid. Added matching CSS (`.repair-trust-strip`, `.repair-why-us`, responsive stacking at both breakpoints). Verified with `typecheck` and `build` — both pass.
- Fixed a real rendering bug in the repair choice drawer (screen/battery quality tiers): the shared `.repair-option-card strong` CSS selector was matching *any* `<strong>` inside the card, including the drawer's own option titles, and yanking them into `position: absolute` — causing choice titles like "Genuine Screen" to float and overlap the first choice card. Rescoped that selector (and its 3 other occurrences) to `.repair-option-link > strong` so it only ever targets the price badge. Rebuilt the choice drawer as clean bordered cards with real star ratings (e.g. `★★★★☆`) instead of "X/5 quality" text.
- Fixed a selection logic bug: choosing a different quality tier (e.g. Premium Screen after Aftermarket Screen) was adding a second line item instead of replacing the first, letting a customer end up with two screens in one booking. `selectRepair` now drops any existing selection for the same repair option before adding the new tier — one quality choice per repair, as intended.
- Redesigned the device-selected header: merged the previously separate dark trust-strip box into the same hero card as the device image/title (one cohesive block with an internal divider, instead of two stacked dark boxes). Rebuilt the back button as a clean fixed 44px circle — the old oversized/glowing look was caused by a leftover high-specificity `.repair-selected-head button` rule fighting the new button's own styling; removed the dead rule and gave the button its own scoped class (`.repair-back-btn`) plus an accessible label. Upgraded Afterpay/Klarna/Zip badges from plain bordered boxes to colored brand-style pills. Verified with `typecheck` and `build` — both pass.
- Replaced the flat line-art repair-part icons (screen/battery/charging/etc.) with 3D-rendered icons from Microsoft's Fluent Emoji set (MIT licensed, free for commercial use — `public/repair-parts/3d/*.png`), matching the polished 3D icon style used by 30minutesfix.com. Removed the old dark square badge frame around each icon (`.repair-visual`) so the colorful 3D icons float cleanly on the card, with a subtle drop-shadow and a hover/selected lift instead.
- Investigated true part-specific 3D icons (e.g. distinct camera-module/back-glass renders like the competitor's) — the free options checked (Fluent Emoji, 3dicons.co) only have generic device icons, not per-part detail; that level of specificity likely needs a paid pack or custom renders. Went with the agreed free workaround instead: reused an existing-but-unused `repairIconLabels` map (LCD/BAT/USB/GLS/CHK/H2O/DAT/PCB/CAM/AUD/OS) as a small theme-matched corner badge (`.repair-visual-tag`, dark pill + lime text) on every repair icon. This directly fixes screen and back-glass looking identical (now LCD vs GLS) and gives every icon a clear text cue to its specific repair type. Verified all 11 image assets are genuine 256×256 transparent PNGs served correctly, and confirmed via the rendered page text that every card shows its distinct badge. `typecheck` and `build` pass.
- Expanded the top brand nav row (`repair-flow-data.ts`) to match 30minutesfix.com's full brand list — was previously just Apple/Samsung/Google/Oppo/Microsoft, with Vivo/Xiaomi/OnePlus/Motorola models incorrectly bundled inside the "Oppo" tab. Split those out into proper standalone brands and added Huawei, Realme, Honor, and Sony as new top-level entries with their own device series, giving 13 brands total in the same order as the reference: Apple, Samsung, Google, OnePlus, Microsoft, Huawei, Oppo, Vivo, Realme, Xiaomi, Honor, Motorola, Sony. Confirmed via the rendered page that all brand buttons appear correctly. `typecheck` and `build` pass.
- Removed the stray "Preowned phones" / "Insurance quote" links from the top brand nav row (`repair-flow.tsx`) — that row is brand-navigation only now.
- Rebuilt device lists to guarantee real, correctly-matched photos: added `cleanStockName()` and `devicesFromStock()` in `repair-flow-data.ts`, which derive each brand's device list directly from the real refurbished-stock photo library (`refurbished-stock.ts`, 228 real Phonebot product photos) instead of a hand-typed model-name list matched via fuzzy string search. Apple (35 devices), Samsung (32), Google (15), Oppo (7), Realme (6, still name-based), Honor (3), Motorola (3), Sony (3, still name-based), Vivo (2), Xiaomi (1), Huawei (1) now show every device with its actual, verified photo — no generic fallback images. Verified via the rendered page HTML that the device grid loads many distinct real image files, not one repeated placeholder.
- **Open item, needs a decision:** OnePlus, Realme, Sony, and Microsoft (laptops) currently have **zero** real photos in the stock library, so those brands still use placeholder/generic images and can't be fixed without either real photos or a scope decision. Logged as a checklist item — see chat for the options given to the owner (drop these brands until real photos exist, source real photos from elsewhere, or keep a clearly-labeled "photo coming soon" placeholder instead of a generic image).
- Added real Smartwatch data for Apple and Samsung — both brands already declared `"Smartwatch"` in `deviceTypes` (so the tab rendered) but had zero series/models, meaning the tab was empty. Sourced 6 real, freely-licensed product photos (Wikimedia Commons, CC BY-SA 4.0) since `refurbished-stock.ts` has no watch category at all: Apple Watch Ultra, Series 10, Series 8, Series 4, and Samsung Galaxy Watch 4 and Galaxy Watch (original 42mm). Downloaded originals, resized/compressed with `sharp` (900px wide, q82) into `public/watches/`, and wired them into `repair-flow-data.ts` via a new `makeDeviceWithImage()` helper. `typecheck` and `build` pass; confirmed all 6 images serve 200 from the dev server.
- **Scope note:** Commons doesn't have clean standalone product photos for every generation (e.g. Galaxy Watch 5/6/Ultra, Apple Watch SE/Series 7/9 either don't exist as standalone shots or are mixed into multi-device group photos) — rather than mislabel a photo as the wrong generation, those were left out for now. Same real-photo-only standard applies to the still-open Tablet/Laptop/Phone gaps below.
- **Rebuilt all of Apple's device catalog** (iPhone, iPad, MacBook, Apple Watch) around Apple's own official device-identification photography instead of Wikimedia Commons/stock-photo matching. `support.apple.com`'s "Identify your ___" pages (`108044` iPhone, `108043` iPad, `108056` Watch, `102869`/`108052` MacBook Air/Pro) serve one clean isolated product shot per generation/variant from `cdsassets.apple.com`, systematically named — exactly the consistent, professional look shown in the reference screenshot, and used here for the purpose Apple itself publishes them for (helping identify a customer's device for service). Downloaded and re-hosted locally (not hotlinked) at `public/apple-official/`, resized to 700px/q85 JPEG via `sharp`. Result: 110 real, visually-consistent devices — 43 iPhones (6 through 17 Pro Max), 23 iPads (7th gen through 2026 Air/Pro), 20 MacBooks (2017 through M5), 24 Apple Watches (Series 3 through 11, SE, Ultra 1-3, split by GPS/Cellular/material where Apple provides distinct shots) — replacing the old stock-photo-derived and generic-fallback lists. `typecheck`/`build` pass; spot-checked images across all 4 categories serve 200.
- **Note on image sourcing / legal basis:** these are Apple's copyrighted marketing images, used here for nominative/descriptive identification (matching a customer's device to a repair), which is standard practice across the repair industry — not a zero-risk guarantee. Flagged to the owner and approved before implementing.
- **Fixed a real bug in the Apple Watch images from the change above:** Apple's `support.apple.com` "identify" images turned out to only exist as a flat "colour options" strip (case + screen only, no band, black inert screen) for Watch — fine for iPhone/iPad/MacBook which have real front+back hero shots, but wrong for Watch. Investigated every free source available (Apple's live compare-page assets, Apple's archived marketing pages via Wayback Machine, Apple's archived buy/compare flow — fully JS-rendered with no static image URLs and blocked from browser network inspection, and Wikipedia infoboxes which just reuse one generic photo for older models) — confirmed Apple's studio-quality "full watch, band visible, lit screen" photography only exists for whatever is currently sold; it's removed entirely once a generation is discontinued, with no freely-available equivalent anywhere.
- **Owner decision:** rather than mix in lower-quality/reused photos for older generations, keep only the ones with the real studio-quality look. Apple Watch now lists exactly 3 models — **Series 11, SE (3rd Gen), Ultra 3** — each with genuine `apple.com` product photography. Older generations (Series 1 through 10, prior SE generations, Ultra, Ultra 2) are intentionally not listed until/unless a matching-quality photo source is found. `typecheck`/`build` pass; all 3 images verified serving 200.
- **Fixed all 43 iPhone images:** they were Apple's "colour options" composite shots (2-6 phones stacked side by side to show every colour in one image) — correct for Apple's own identify page, wrong for a repair-shop catalog that needs one clean phone per card. Cropped every image down to just the single front-facing phone using `sharp`, calibrated in 3 passes (a base crop for most, a wider strip for older models whose composite included a separate side-profile view, and a narrower crop for recent Pro models where a naive crop still caught part of an adjacent color's camera bump). Verified all 43 via a generated contact-sheet QA pass — every model now shows exactly one clean phone. Re-synced to source and to the live dev server; confirmed images load.
- **Note:** the Apple Watch section of this file was independently restructured by another concurrent session while this work was in progress (now a full Series 1–Ultra 3 grid by size, referencing new SVG assets under `public/watches/`) — left untouched/not reverted, since it's someone else's in-progress work.
- **Root-caused the remaining "cropped/thin" complaint:** it wasn't a caching or crop bug — real iPhone photos are narrow (~0.4–0.46 width:height), so forcing them into the device grid's old fixed **square** image box (90×90) left visible white space on the sides, which read as "cropped." Changed `.repair-device-grid img` in `globals.css` to a portrait box (76×130) matching the phone's real proportions instead of reprocessing the images again. Verified the dev server hot-reloaded the change.
- **Fixed genuine content loss on ~13 of the 43 iPhone crops** (mostly iPhone 12 through 17, Air, 16e/17e): re-downloaded the original Apple composite images fresh (the first crop pass had overwritten the sources in place) and found the real cause — iPhone 11 onward, Apple's "colour options" composite images drop the separate side-profile view that iPhone 6 through XS/SE(2020) include, so the crop formula tuned for the older 3-element composites (colours + front + profile) was cutting into the front phone's camera bump for the newer 2-element composites (colours + front only, no profile). Split into two correctly-calibrated formulas by generation and re-cropped, re-squared, and re-verified all 43 via a full contact-sheet QA pass plus direct full-resolution spot checks on the worst previous offenders (17 Pro, 17, 16 Pro Max, Air) — all show one complete, correctly-framed phone now. Re-synced to source and confirmed serving from the dev server.
